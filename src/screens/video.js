import { auth } from '../features/auth/firebase-client.js';
import { renderIndoBrandTopbar } from '../components/indo-brand-topbar.js';

const STYLE_ID='indo-video-section-v218';
const FOLLOWED_TTL_MS=24*60*60*1000;
const SEEN_STORAGE_KEY='indo:followed-video-seen:v2';
const API_BASE=()=>window.INDO_API_BASE||'';

function esc(value=''){return String(value).replace(/[&<>\"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));}
function formatCount(value){const n=Number(value||0);if(n>=1000000)return`${(n/1000000).toFixed(n>=10000000?0:1)}M`;if(n>=1000)return`${(n/1000).toFixed(n>=10000?0:1)}K`;return String(Math.max(0,n));}
function formatAge(timestamp){const diff=Math.max(0,Date.now()-Number(timestamp||Date.now()));const mins=Math.floor(diff/60000);if(mins<1)return'Just now';if(mins<60)return`${mins}m ago`;const hours=Math.floor(mins/60);if(hours<24)return`${hours}h ago`;const days=Math.floor(hours/24);if(days<7)return`${days}d ago`;return new Date(Number(timestamp)).toLocaleDateString();}
function formatDuration(seconds){const n=Number(seconds||0);if(!n)return'';return`${Math.floor(n/60)}:${String(Math.floor(n%60)).padStart(2,'0')}`;}
function videoUrl(raw){const url=String(raw||'').trim();if(!url)return'';if(!url.includes('res.cloudinary.com')||!url.includes('/video/upload/'))return url;const marker='/video/upload/';const i=url.indexOf(marker);if(i<0)return url;const prefix=url.slice(0,i+marker.length),rest=url.slice(i+marker.length);if(rest.startsWith('f_mp4,vc_h264,ac_aac/'))return url;return`${prefix}f_mp4,vc_h264,ac_aac/${rest}`;}
function getSeenMap(){try{const raw=localStorage.getItem(SEEN_STORAGE_KEY);const parsed=raw?JSON.parse(raw):{};return parsed&&typeof parsed==='object'?parsed:{}}catch{return{}}}
function saveSeenMap(map){try{localStorage.setItem(SEEN_STORAGE_KEY,JSON.stringify(map))}catch{}}
function pruneSeenMap(videos){const now=Date.now();const validIds=new Set(videos.map(v=>String(v.id||'')));const map=getSeenMap();let changed=false;for(const[id,seenAt]of Object.entries(map)){if(!validIds.has(id)||now-Number(seenAt)>FOLLOWED_TTL_MS){delete map[id];changed=true}}if(changed)saveSeenMap(map);return map;}
function markSeen(id){if(!id)return;const map=getSeenMap();map[String(id)]=Date.now();saveSeenMap(map);}
function creatorKeys(item){return new Set([String(item.ownerUid||''),String(item.uid||''),String(item.creatorUid||''),String(item.userId||'').replace(/^@/,''),String(item.creator||'').replace(/^@/,'')].filter(Boolean));}
function followingKeys(item){return new Set([String(item.uid||''),String(item.userId||'').replace(/^@/,''),String(item.username||'').replace(/^@/,''),String(item.creatorUid||'')].filter(Boolean));}

function installStyles(){if(document.getElementById(STYLE_ID))return;const s=document.createElement('style');s.id=STYLE_ID;s.textContent=`
.indo-video-shell{min-height:100vh;background:#030308;color:#fff;padding-bottom:78px;overflow-x:hidden}.indo-video-main{padding:10px 10px 24px;max-width:720px;margin:0 auto}.indo-video-search{width:100%;height:44px;border:1px solid #2b2634;border-radius:13px;background:#111118;color:#fff;display:flex;align-items:center;gap:9px;padding:0 13px;box-sizing:border-box;margin:0 0 16px}.indo-video-search svg{width:19px;height:19px;stroke:#dedbe5;fill:none;stroke-width:1.8;flex:none}.indo-video-search input{flex:1;min-width:0;border:0;outline:0;background:transparent;color:#fff;font-size:12px}.indo-video-search input::placeholder{color:#777381}.indo-video-follow-head,.indo-video-recent-head{display:flex;align-items:end;justify-content:space-between;gap:10px;margin:0 2px 10px}.indo-video-follow-head h2,.indo-video-recent-head h2{margin:0;font-size:16px;font-weight:900}.indo-video-follow-head small,.indo-video-recent-head small{display:block;margin-top:3px;color:#888392;font-size:8px}.indo-video-follow-row{display:flex;gap:8px;overflow-x:auto;overflow-y:hidden;padding:0 2px 13px;margin-bottom:15px;border-bottom:1px solid #191820;scroll-snap-type:x mandatory;scrollbar-width:none;touch-action:pan-x}.indo-video-follow-row::-webkit-scrollbar{display:none}.indo-video-mini{position:relative;min-width:112px;width:112px;flex:0 0 112px;text-align:left;border:0;background:transparent;color:#fff;padding:0;scroll-snap-align:start;cursor:pointer}.indo-video-mini-media{position:relative;width:100%;aspect-ratio:3/4;border:1px solid #2a2530;border-radius:11px;overflow:hidden;background:#0d0d13}.indo-video-mini.is-new .indo-video-mini-media{border-color:#d636ff;box-shadow:0 0 0 1px rgba(214,54,255,.9),0 0 14px rgba(235,47,191,.26),0 0 22px rgba(119,57,255,.18)}.indo-video-mini-media video{width:100%;height:100%;display:block;object-fit:cover;background:#111}.indo-video-mini-duration{position:absolute;right:5px;bottom:5px;padding:3px 4px;border-radius:5px;background:rgba(0,0,0,.68);font-size:7px;font-weight:800}.indo-video-mini-name{display:block;margin-top:6px;font-size:8px;font-weight:800;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.indo-video-mini-time{display:block;margin-top:2px;color:#777381;font-size:7px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.indo-video-follow-empty{width:100%;padding:18px;text-align:center;border:1px dashed #292532;border-radius:11px;color:#777381;font-size:9px;box-sizing:border-box}.indo-video-recent-head{margin-top:2px}.indo-video-recent-head h2{display:flex;align-items:center;gap:7px}.indo-video-recent-head h2 span{color:#ff35be}.indo-video-recent-count{color:#777381;font-size:8px;margin-left:auto}.indo-video-list{display:grid;gap:13px}.indo-video-card{position:relative;border:1px solid #25242d;border-radius:13px;overflow:hidden;background:#09090e;box-shadow:0 9px 25px rgba(0,0,0,.24)}.indo-video-poster{position:relative;width:100%;aspect-ratio:16/9;background:#111;overflow:hidden;border:0;padding:0;display:block;cursor:pointer}.indo-video-poster video{width:100%;height:100%;display:block;object-fit:cover;background:#111}.indo-video-duration{position:absolute;right:8px;bottom:8px;padding:4px 6px;border-radius:6px;background:rgba(0,0,0,.72);font-size:8px;font-weight:800}.indo-video-title-block{padding:9px 10px 10px}.indo-video-title{margin:0;font-size:14px;font-weight:900;line-height:1.25}.indo-video-meta-line{display:flex;align-items:center;gap:6px;margin-top:5px;color:#8a8593;font-size:8px}.indo-video-user-id{color:#d4d0da;font-weight:800}.indo-video-menu{margin-left:auto;border:0;background:transparent;color:#cfcad7;font-size:18px;line-height:1;cursor:pointer}.indo-video-no-results{padding:30px 14px;text-align:center;color:#85808e;font-size:11px;border:1px dashed #2a2733;border-radius:13px;background:rgba(9,9,14,.72)}.indo-video-viewer{position:fixed;inset:0;z-index:50000;background:rgba(0,0,0,.94);display:grid;place-items:center}.indo-video-viewer-card{width:min(100%,560px);height:100dvh;background:#05050a;display:flex;flex-direction:column;overflow:hidden}.indo-video-viewer-top{height:54px;display:flex;align-items:center;justify-content:space-between;padding:0 12px;box-sizing:border-box;color:#fff}.indo-video-viewer-top button{width:34px;height:34px;border:0;border-radius:50%;background:rgba(255,255,255,.07);color:#fff;font-size:22px}.indo-video-viewer-player{width:100%;aspect-ratio:16/9;background:#000;display:grid;place-items:center}.indo-video-viewer-player video{width:100%;height:100%;object-fit:contain;background:#000}.indo-video-viewer-info{padding:12px}.indo-video-viewer-info h2{margin:0;font-size:17px}.indo-video-viewer-info p{margin:6px 0 0;color:#aaa4b2;font-size:10px;line-height:1.45}.indo-video-viewer-actions{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:13px}.indo-video-viewer-actions button{height:42px;border:1px solid #2a2732;border-radius:10px;background:#0d0d14;color:#fff;font-size:10px;font-weight:800}.indo-video-viewer-comments{margin-top:13px;padding-top:12px;border-top:1px solid #1e1c24;color:#b6b0bf;font-size:10px}@media(max-width:420px){.indo-video-main{padding:9px 8px 20px}.indo-video-search{height:43px}.indo-video-title{font-size:13px}}
`;document.head.appendChild(s)}

async function authedFetch(path,options={}){const user=auth.currentUser;if(!user)throw new Error('Please login first.');const token=await user.getIdToken();return fetch(`${API_BASE()}${path}`,{...options,headers:{...(options.headers||{}),Authorization:`Bearer ${token}`}})}
async function loadFollowing(){try{const uid=String(auth.currentUser?.uid||'').trim();if(!uid)return[];const response=await authedFetch(`/api/social/following/${encodeURIComponent(uid)}`);const data=await response.json().catch(()=>({}));return response.ok&&Array.isArray(data.items)?data.items:[]}catch{return[]}}
async function loadVideos(){const response=await fetch(`${API_BASE()}/api/media/videos?limit=50&type=video`);const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Could not load videos.');return Array.isArray(data.videos)?data.videos:[]}

function followedVideos(videos,following){const keys=new Set(following.flatMap(f=>[...followingKeys(f)]));const cutoff=Date.now()-FOLLOWED_TTL_MS;return videos.filter(v=>Number(v.createdAt||0)>=cutoff&&[...creatorKeys(v)].some(k=>keys.has(k)));}
function orderFollowed(videos){const seen=pruneSeenMap(videos);return[...videos].sort((a,b)=>{const aSeen=Number(seen[String(a.id)])||0;const bSeen=Number(seen[String(b.id)])||0;if(Boolean(aSeen)!==Boolean(bSeen))return aSeen?1:-1;return Number(b.createdAt||0)-Number(a.createdAt||0);});}
function searchMatch(video,query){const q=String(query||'').trim().toLowerCase();if(!q)return true;return[video.title,video.caption,video.creator,video.creatorName,video.userId,video.username].map(v=>String(v||'').toLowerCase()).join(' ').includes(q);}

function renderMini(video,seen){const raw=video.secureUrl||video.videoUrl||video.url||'';const src=videoUrl(raw);const creator=String(video.creator||video.userId||'@user').replace(/^@/,'');const creatorName=String(video.creatorName||'Indo User');const dur=formatDuration(video.duration);return`<button class="indo-video-mini ${seen?'':'is-new'}" type="button" data-video-open="${esc(video.id||'')}"><div class="indo-video-mini-media"><video playsinline preload="metadata" muted src="${esc(src)}" data-original-src="${esc(raw)}"></video>${dur?`<span class="indo-video-mini-duration">${esc(dur)}</span>`:''}</div><b class="indo-video-mini-name">@${esc(creator)}</b><small class="indo-video-mini-time">${esc(creatorName)} · ${esc(formatAge(video.createdAt))}</small></button>`;}
function renderPoster(video){const raw=video.secureUrl||video.videoUrl||video.url||'';const src=videoUrl(raw);const title=String(video.title||'Untitled video');const creator=String(video.creator||video.userId||'@user').replace(/^@/,'');const dur=formatDuration(video.duration);return`<article class="indo-video-card"><button class="indo-video-poster" type="button" data-video-open="${esc(video.id||'')}"><video playsinline preload="metadata" muted src="${esc(src)}" data-original-src="${esc(raw)}"></video>${dur?`<span class="indo-video-duration">${esc(dur)}</span>`:''}</button><div class="indo-video-title-block"><h3 class="indo-video-title">${esc(title)}</h3><div class="indo-video-meta-line"><span class="indo-video-user-id">@${esc(creator)}</span><span>·</span><span>${esc(formatAge(video.createdAt))}</span><span>·</span><span>${formatCount(video.views)} views</span><button class="indo-video-menu" type="button" aria-label="More">⋮</button></div></div></article>`;}
function openViewer(video,onClose){document.querySelector('.indo-video-viewer')?.remove();markSeen(video.id);const raw=String(video.secureUrl||video.videoUrl||video.url||'');const src=videoUrl(raw);const viewer=document.createElement('div');viewer.className='indo-video-viewer';viewer.innerHTML=`<div class="indo-video-viewer-card"><div class="indo-video-viewer-top"><button type="button" data-close>‹</button><strong>Video</strong><button type="button" data-close>×</button></div><div class="indo-video-viewer-player"><video controls playsinline preload="auto" src="${esc(src)}"></video></div><div class="indo-video-viewer-info"><h2>${esc(video.title||'Untitled video')}</h2><p>@${esc(String(video.creator||video.userId||'user').replace(/^@/,''))} · ${esc(formatAge(video.createdAt))}</p><div class="indo-video-viewer-actions"><button type="button">♡ ${formatCount(video.likes)}</button><button type="button">💬 ${formatCount(video.comments)}</button><button type="button">↗ Share</button><button type="button">🔖 Save</button></div><div class="indo-video-viewer-comments">${formatCount(video.views)} views · Like, comment, share and save are available from the player.</div></div></div>`;document.body.appendChild(viewer);viewer.querySelectorAll('[data-close]').forEach(btn=>btn.addEventListener('click',()=>{viewer.remove();onClose?.()}));viewer.addEventListener('click',event=>{if(event.target===viewer){viewer.remove();onClose?.()}});viewer.querySelector('video')?.play().catch(()=>{});}
function bindOpeners(app,allVideos,onFollowedSeen){const map=new Map(allVideos.map(v=>[String(v.id),v]));app.querySelectorAll('[data-video-open]').forEach(btn=>btn.addEventListener('click',()=>{const video=map.get(String(btn.dataset.videoOpen));if(!video)return;const isFollowed=Boolean(btn.closest('.indo-video-mini'));openViewer(video,isFollowed?onFollowedSeen:undefined);}));app.querySelectorAll('.indo-video-mini-media video,.indo-video-poster video').forEach(v=>v.addEventListener('error',()=>{const raw=String(v.dataset.originalSrc||'');if(raw&&v.src!==raw){v.src=raw;v.load();}}));}
function installSwipe(row){let startX=0;let dragging=false;row.addEventListener('touchstart',e=>{startX=e.changedTouches[0]?.clientX||0;dragging=true;},{passive:true});row.addEventListener('touchend',e=>{if(!dragging)return;const endX=e.changedTouches[0]?.clientX||0;dragging=false;const delta=endX-startX;if(Math.abs(delta)<35)return;const cards=[...row.querySelectorAll('.indo-video-mini')];if(!cards.length)return;const rowRect=row.getBoundingClientRect();const current=Math.max(0,cards.findIndex(card=>{const rect=card.getBoundingClientRect();return rect.left>=rowRect.left-2&&rect.left<rowRect.left+rowRect.width/2;}));const next=Math.max(0,Math.min(cards.length-1,current+(delta<0?1:-1)));cards[next]?.scrollIntoView({behavior:'smooth',inline:'start',block:'nearest'});},{passive:true});}

export async function renderVideo(app){
  installStyles();
  const topbar=renderIndoBrandTopbar({rightHtml:'<button type="button" data-screen="create" aria-label="Create">＋</button>',rightLabel:'Create'});
  app.innerHTML=`<div class="app-shell indo-video-shell">${topbar}<main class="indo-video-main"><label class="indo-video-search" aria-label="Search videos"><svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"></circle><path d="m20 20-4-4"></path></svg><input data-video-search type="search" autocomplete="off" placeholder="Search videos, users, topics..." /></label><section><div class="indo-video-follow-head"><div><h2>From people you follow</h2><small>Latest videos · 24 hours</small></div></div><div class="indo-video-follow-row" data-follow-list><div class="indo-video-follow-empty">Loading...</div></div></section><section><div class="indo-video-recent-head"><div><h2><span>ϟ</span> Recent uploads</h2><small>Latest video uploads from everyone</small></div><span class="indo-video-recent-count" data-result-count></span></div><div class="indo-video-list" data-video-list><div class="indo-video-no-results">Loading videos...</div></div></section></main></div>`;
  const followList=app.querySelector('[data-follow-list]');const list=app.querySelector('[data-video-list]');const searchInput=app.querySelector('[data-video-search]');const resultCount=app.querySelector('[data-result-count]');
  try{
    const [videos,following]=await Promise.all([loadVideos(),loadFollowing()]);
    const drawFollowed=(query='')=>{const matches=orderFollowed(followedVideos(videos,following).filter(v=>searchMatch(v,query)));const seen=pruneSeenMap(videos);followList.innerHTML=matches.length?matches.map(v=>renderMini(v,Boolean(seen[String(v.id)]) )).join(''):'<div class="indo-video-follow-empty">No followed videos in the last 24 hours.</div>';installSwipe(followList);bindOpeners(app,videos,drawFollowed);};
    const drawRecent=(query='')=>{const filtered=videos.filter(v=>searchMatch(v,query)).sort((a,b)=>Number(b.createdAt||0)-Number(a.createdAt||0));resultCount.textContent=query?`${filtered.length} video${filtered.length===1?'':'s'}`:'';list.innerHTML=filtered.length?filtered.map(renderPoster).join(''):`<div class="indo-video-no-results">No videos found for “${esc(query)}”.</div>`;bindOpeners(app,videos,drawFollowed);};
    drawFollowed('');drawRecent('');searchInput.addEventListener('input',()=>{const query=searchInput.value;drawFollowed(query);drawRecent(query);});
  }catch(error){followList.innerHTML='<div class="indo-video-follow-empty">Could not load followed videos.</div>';list.innerHTML=`<div class="indo-video-no-results">${esc(error?.message||'Could not load videos.')}</div>`;}
}
