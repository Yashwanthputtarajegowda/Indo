import { auth } from '../features/auth/firebase-client.js';
import { renderIndoBrandTopbar } from '../components/indo-brand-topbar.js';
import { nav } from '../components/nav.js';

const STYLE_ID='indo-profile-live-v249';
const API_BASE=()=>window.INDO_API_BASE||'';
const POLL_MS=3000;

const esc=(v='')=>String(v).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
const count=v=>{const n=Number(v)||0;if(n>=1e6)return`${(n/1e6).toFixed(1).replace('.0','')}M`;if(n>=1e3)return`${(n/1e3).toFixed(1).replace('.0','')}K`;return String(n)};
async function token(){const u=auth.currentUser;if(!u)throw new Error('Please login first.');return u.getIdToken(false)}
async function api(path,options={}){const headers={...(options.headers||{})};if(options.body!==undefined)headers['Content-Type']='application/json';if(options.auth!==false)headers.Authorization=`Bearer ${await token()}`;const r=await fetch(`${API_BASE()}${path}`,{...options,headers,cache:'no-store'});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.error||'Request failed.');return d}
function installStyles(){if(document.getElementById(STYLE_ID))return;const s=document.createElement('style');s.id=STYLE_ID;s.textContent=`.pl249{width:min(100%,520px);min-height:100vh;margin:0 auto;background:#05070d;color:#f7f8ff;padding-bottom:82px;overflow-x:hidden}.pl249-main{padding:12px 16px 28px}.pl249-hero{text-align:center;padding:12px 0 4px}.pl249-pic{position:relative;width:132px;height:132px;margin:8px auto 13px;display:grid;place-items:center}.pl249-ring{position:absolute;inset:0;border-radius:50%;background:conic-gradient(#6b4cff,#c83cff,#ff3e9c,#37b8ff,#6b4cff);box-shadow:0 0 28px rgba(125,70,255,.2)}.pl249-ring:after{content:'';position:absolute;inset:4px;border-radius:50%;background:#05070d}.pl249-avatar{position:relative;z-index:1;width:116px;height:116px;border-radius:50%;overflow:hidden;background:#171b28;border:2px solid #111625;display:grid;place-items:center;font-size:42px;font-weight:900}.pl249-avatar img{width:100%;height:100%;object-fit:cover}.pl249-online{position:absolute;right:3px;bottom:10px;z-index:2;width:14px;height:14px;border-radius:50%;background:#2cdf8a;border:3px solid #05070d}.pl249-name{font-size:25px;font-weight:900;line-height:1.1}.pl249-id{margin-top:5px;color:#8e96aa;font-size:13px}.pl249-bio{max-width:370px;margin:12px auto 0;color:#d9ddea;font-size:13px;line-height:1.45}.pl249-location{display:inline-flex;margin-top:10px;padding:7px 12px;border:1px solid #252b3d;border-radius:999px;background:#0b0f1a;color:#9ca4b6;font-size:11px}.pl249-line{height:1px;background:#171d2a;margin:20px 0 18px}.pl249-stats{display:grid;grid-template-columns:repeat(4,1fr);border-top:1px solid #171d2a;border-bottom:1px solid #171d2a}.pl249-stat{border:0;border-right:1px solid #171d2a;background:transparent;color:#fff;padding:12px 4px}.pl249-stat:last-child{border-right:0}.pl249-stat b{display:block;font-size:17px}.pl249-stat span{display:block;margin-top:4px;color:#7f8798;font-size:9px;text-transform:uppercase;letter-spacing:.7px}.pl249-actions{display:grid;grid-template-columns:1.9fr 1fr 46px;gap:8px;margin:18px 0 22px}.pl249-btn{height:40px;border:1px solid #2a3042;border-radius:11px;background:#0b0f19;color:#fff;font-size:12px;font-weight:800;cursor:pointer}.pl249-btn.primary{border:0;background:linear-gradient(105deg,#6748ff,#b43ce7,#ef3c9e);box-shadow:0 9px 24px rgba(156,58,226,.2)}.pl249-section-title{display:flex;justify-content:space-between;align-items:center;margin-bottom:11px}.pl249-section-title strong{font-size:16px}.pl249-section-title button{border:0;background:transparent;color:#a778ff;font-size:11px;font-weight:800}.pl249-videos{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.pl249-video{position:relative;aspect-ratio:4/5;border:1px solid #1e2534;border-radius:12px;overflow:hidden;background:#111625;padding:0}.pl249-video img,.pl249-video video{width:100%;height:100%;object-fit:cover}.pl249-view{position:absolute;left:8px;bottom:7px;color:#fff;font-size:9px;font-weight:900}.pl249-empty{padding:44px 12px;text-align:center;color:#7e8799;border:1px dashed #20283a;border-radius:14px;grid-column:1/-1}.pl249-other{display:grid;grid-template-columns:1.5fr 1fr 46px;gap:8px;margin-top:18px}`;document.head.appendChild(s)}
function avatar(profile){const url=String(profile?.avatarUrl||profile?.photoURL||profile?.photoUrl||'').trim();if(url)return`<div class="pl249-avatar"><img src="${esc(url)}" alt="${esc(profile?.name||'Profile')}"></div>`;const initial=(String(profile?.name||profile?.userId||'I').replace(/^@/,'').trim().charAt(0)||'I').toUpperCase();return`<div class="pl249-avatar">${esc(initial)}</div>`}
async function loadProfile(userId){const id=String(userId||'').replace(/^@/,'').trim();if(!id)return null;const d=await api(`/api/account/profile/${encodeURIComponent(id)}`);return d?.profile?{...d.profile,stats:d.stats||{},social:d.social||{}}:null}
async function loadOwnProfile(){const d=await api('/api/account/me');return d?.profile?{...d.profile,stats:d.stats||{},social:d.social||{}}:null}
async function loadVideos(uid){if(!uid)return[];try{const d=await api('/api/media/videos?limit=50',{auth:false});return Array.isArray(d?.videos)?d.videos.filter(v=>String(v.ownerUid||'')===String(uid)):[]}catch{return[]}}
function videoGrid(items){if(!items.length)return'<div class="pl249-empty">No videos uploaded yet.</div>';return items.slice(0,9).map(v=>`<button class="pl249-video" type="button" data-video="${esc(v.secureUrl||v.videoUrl||v.url||'')}" data-title="${esc(v.title||'')}">${v.thumbnailUrl?`<img src="${esc(v.thumbnailUrl)}" alt="" loading="lazy">`:`<video src="${esc(v.secureUrl||v.videoUrl||v.url||'')}" muted playsinline preload="metadata"></video>`}<span class="pl249-view">${esc(count(v.views))} views</span></button>`).join('')}
function openVideo(url,title){if(!url)return;const o=document.createElement('div');o.style.cssText='position:fixed;inset:0;z-index:30000;background:#000;display:grid;place-items:center';o.innerHTML=`<div style="position:relative;width:min(100%,520px);height:100vh"><button style="position:absolute;z-index:2;top:14px;left:14px;width:38px;height:38px;border:0;border-radius:50%;background:rgba(0,0,0,.7);color:#fff;font-size:24px">×</button><div style="position:absolute;z-index:2;top:20px;left:64px;color:#fff;font-size:13px;font-weight:800">${esc(title||'Indo video')}</div><video src="${esc(url)}" controls autoplay playsinline style="width:100%;height:100%;object-fit:contain"></video></div>`;document.body.appendChild(o);o.querySelector('button').onclick=()=>o.remove()}
async function follow(uid,b){try{const st=await api(`/api/social/follow-status/${encodeURIComponent(uid)}`);const next=!Boolean(st.following);b.disabled=true;const d=await api('/api/social/follow',{method:'POST',body:JSON.stringify({targetUid:uid,follow:next})});b.textContent=d?.pending?'Requested':d?.following?'Following':'Follow';window.dispatchEvent(new CustomEvent('indo:profile-stats-changed'))}catch(e){b.textContent=e.message||'Follow'}finally{b.disabled=false}}
export async function renderProfile(app,profileArg=null){
  installStyles();
  if(window.__indoProfileLiveTimer){clearInterval(window.__indoProfileLiveTimer);window.__indoProfileLiveTimer=null}
  if(window.__indoProfileLiveRefresh){document.removeEventListener('visibilitychange',window.__indoProfileLiveRefresh);window.__indoProfileLiveRefresh=null}
  const currentUid=String(auth.currentUser?.uid||'');const requestedUid=String(profileArg?.uid||profileArg?.ownerUid||'').trim();const requestedId=String(profileArg?.userId||profileArg?.username||'').replace(/^@/,'').trim();let profile=null;let own=false;
  try{if(!requestedUid&&!requestedId){own=true;profile=await loadOwnProfile()}else if(requestedUid&&requestedUid===currentUid){own=true;profile=await loadOwnProfile()}else if(requestedId){profile=await loadProfile(requestedId);own=Boolean(currentUid&&profile?.uid&&String(profile.uid)===currentUid);if(own)profile=await loadOwnProfile()}if(!profile)throw new Error('Profile could not be loaded.')}catch(e){app.innerHTML=`<div class="pl249"><main class="pl249-main"><h2>Profile unavailable</h2><p>${esc(e.message)}</p></main>${nav('profile')}</div>`;return}
  const authName=String(auth.currentUser?.displayName||'').trim();
  const rawName=String(profile.name||profile.displayName||'').trim();
  const name=own && authName && (!rawName||rawName.toLowerCase()==='indo user') ? authName : (rawName||authName||'Indo User');
  const uid=String(profile.uid||profile.ownerUid||currentUid);const id=String(profile.userId||profile.username||'').replace(/^@/,'');const bio=String(profile.bio||profile.about||'').trim();const location=String(profile.location||'').trim();const stats=profile.stats||{};const social=profile.social||{};const vcount=Number(stats.videosCount??profile.videosCount??stats.postsCount??0);const followers=Number(stats.followersCount??social.followersCount??profile.followersCount??0);const following=Number(stats.followingCount??social.followingCount??profile.followingCount??0);const likes=Number(stats.likesCount??profile.likesCount??0);const items=await loadVideos(uid);
  app.innerHTML=`<div class="pl249">${renderIndoBrandTopbar({rightLabel:'Profile'})}<main class="pl249-main"><section class="pl249-hero"><div class="pl249-pic"><div class="pl249-ring"></div>${avatar({...profile,name})}<span class="pl249-online"></span></div><div class="pl249-name">${esc(name)}${profile.verified?'<span class="pl249-verified">✓</span>':''}</div><div class="pl249-id">@${esc(id)}</div>${bio?`<div class="pl249-bio">${esc(bio)}</div>`:''}${location?`<div class="pl249-location">⌖ ${esc(location)}</div>`:''}</section><div class="pl249-line"></div><section class="pl249-stats"><div class="pl249-stat"><b data-live-stat="videos">${count(vcount)}</b><span>Videos</span></div><div class="pl249-stat"><b data-live-stat="followers">${count(followers)}</b><span>Followers</span></div><div class="pl249-stat"><b data-live-stat="following">${count(following)}</b><span>Following</span></div><div class="pl249-stat"><b data-live-stat="likes">${count(likes)}</b><span>Likes</span></div></section>${own?`<div class="pl249-actions"><button class="pl249-btn primary" type="button" data-screen="edit-profile">✎ Edit Profile</button><button class="pl249-btn" type="button" data-profile-share>Share</button><button class="pl249-btn" type="button" data-screen="settings">⚙</button></div>`:`<div class="pl249-other"><button class="pl249-btn primary" type="button" data-follow="${esc(uid)}">Follow</button><button class="pl249-btn" type="button" data-screen="messages">Message</button><button class="pl249-btn" type="button" data-profile-share>↗</button></div>`}<section><div class="pl249-section-title"><strong>${own?'Recent Videos':'Videos'}</strong><button type="button" data-screen="video">View all</button></div><div class="pl249-videos">${videoGrid(items)}</div></section></main>${nav('profile')}</div>`;
  app.querySelectorAll('[data-video]').forEach(b=>b.addEventListener('click',()=>openVideo(b.dataset.video,b.dataset.title)));app.querySelectorAll('[data-profile-share]').forEach(b=>b.addEventListener('click',async()=>{const url=`${location.origin}${location.pathname}?profile=${encodeURIComponent(id)}`;try{if(navigator.share)await navigator.share({title:`${name} on Indo`,url});else if(navigator.clipboard)await navigator.clipboard.writeText(url)}catch{}}));app.querySelectorAll('[data-follow]').forEach(b=>b.addEventListener('click',()=>follow(b.dataset.follow,b)));
  let refreshing=false;const refreshStats=async()=>{if(refreshing||document.hidden)return;refreshing=true;try{const fresh=own?await loadOwnProfile():await loadProfile(id);if(!fresh)return;const s=fresh.stats||{},soc=fresh.social||{};const next={videos:Number(s.videosCount??fresh.videosCount??s.postsCount??0),followers:Number(s.followersCount??soc.followersCount??fresh.followersCount??0),following:Number(s.followingCount??soc.followingCount??fresh.followingCount??0),likes:Number(s.likesCount??fresh.likesCount??0)};Object.entries(next).forEach(([k,v])=>{const el=app.querySelector(`[data-live-stat="${k}"]`);if(el)el.textContent=count(v)})}catch(e){console.warn('Live profile stats refresh failed:',e)}finally{refreshing=false}};
  window.__indoProfileLiveTimer=setInterval(refreshStats,POLL_MS);window.__indoProfileLiveRefresh=()=>{if(!document.hidden)refreshStats()};document.addEventListener('visibilitychange',window.__indoProfileLiveRefresh);window.addEventListener('indo:profile-stats-changed',refreshStats);refreshStats();
}
