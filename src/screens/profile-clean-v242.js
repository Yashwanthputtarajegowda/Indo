import { auth } from '../features/auth/firebase-client.js';
import { renderIndoBrandTopbar } from '../components/indo-brand-topbar.js';
import { nav } from '../components/nav.js';

const STYLE_ID = 'indo-profile-clean-v242';
const API_BASE = () => window.INDO_API_BASE || '';

function esc(value = '') { return String(value).replace(/[&<>\"']/g, (c) => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '\"':'&quot;', "'":'&#039;' }[c])); }
function count(value) { const n = Number(value) || 0; if (n >= 1e6) return `${(n/1e6).toFixed(1).replace('.0','')}M`; if (n >= 1e3) return `${(n/1e3).toFixed(1).replace('.0','')}K`; return String(n); }
async function token() { const user = auth.currentUser; if (!user) throw new Error('Please login first.'); return user.getIdToken(true); }
async function api(path, options = {}) { const headers = { ...(options.headers || {}) }; if (options.body !== undefined) headers['Content-Type'] = 'application/json'; if (options.auth !== false) headers.Authorization = `Bearer ${await token()}`; const r = await fetch(`${API_BASE()}${path}`, { ...options, headers, cache:'no-store' }); const d = await r.json().catch(()=>({})); if (!r.ok) throw new Error(d.error || 'Request failed.'); return d; }
function avatar(profile) { const url = String(profile?.avatarUrl || profile?.photoURL || profile?.photoUrl || '').trim(); if (url) return `<div class="pc242-avatar"><img src="${esc(url)}" alt="${esc(profile?.name || 'Profile')}"></div>`; const initial = (String(profile?.name || profile?.userId || 'I').replace(/^@/,'').trim().charAt(0) || 'I').toUpperCase(); return `<div class="pc242-avatar">${esc(initial)}</div>`; }
function styles() { if (document.getElementById(STYLE_ID)) return; const s=document.createElement('style'); s.id=STYLE_ID; s.textContent=`
.pc242{width:min(100%,520px);min-height:100vh;margin:0 auto;background:#05070d;color:#f7f8ff;padding-bottom:82px;overflow-x:hidden}.pc242-main{padding:12px 16px 28px}.pc242-hero{text-align:center;padding:12px 0 4px}.pc242-pic{position:relative;width:132px;height:132px;margin:8px auto 13px;display:grid;place-items:center}.pc242-ring{position:absolute;inset:0;border-radius:50%;background:conic-gradient(#6b4cff,#c83cff,#ff3e9c,#37b8ff,#6b4cff);box-shadow:0 0 28px rgba(125,70,255,.2)}.pc242-ring:after{content:'';position:absolute;inset:4px;border-radius:50%;background:#05070d}.pc242-avatar{position:relative;z-index:1;width:116px;height:116px;border-radius:50%;overflow:hidden;background:#171b28;border:2px solid #111625;display:grid;place-items:center;font-size:42px;font-weight:900}.pc242-avatar img{width:100%;height:100%;object-fit:cover}.pc242-online{position:absolute;right:3px;bottom:10px;z-index:2;width:14px;height:14px;border-radius:50%;background:#2cdf8a;border:3px solid #05070d}.pc242-name{font-size:25px;font-weight:900;line-height:1.1}.pc242-verified{display:inline-grid;place-items:center;width:18px;height:18px;margin-left:5px;border-radius:50%;background:linear-gradient(135deg,#6b48ff,#be38e8);font-size:10px;vertical-align:4px}.pc242-id{margin-top:5px;color:#8e96aa;font-size:13px}.pc242-bio{max-width:370px;margin:12px auto 0;color:#d9ddea;font-size:13px;line-height:1.45}.pc242-location{display:inline-flex;margin-top:10px;padding:7px 12px;border:1px solid #252b3d;border-radius:999px;background:#0b0f1a;color:#9ca4b6;font-size:11px}.pc242-line{height:1px;background:#171d2a;margin:20px 0 18px}.pc242-stats{display:grid;grid-template-columns:repeat(4,1fr);border-top:1px solid #171d2a;border-bottom:1px solid #171d2a}.pc242-stat{border:0;border-right:1px solid #171d2a;background:transparent;color:#fff;padding:12px 4px}.pc242-stat:last-child{border-right:0}.pc242-stat b{display:block;font-size:17px}.pc242-stat span{display:block;margin-top:4px;color:#7f8798;font-size:9px;text-transform:uppercase;letter-spacing:.7px}.pc242-actions{display:grid;grid-template-columns:1.9fr 1fr 46px;gap:8px;margin:18px 0 22px}.pc242-btn{height:40px;border:1px solid #2a3042;border-radius:11px;background:#0b0f19;color:#fff;font-size:12px;font-weight:800;cursor:pointer}.pc242-btn.primary{border:0;background:linear-gradient(105deg,#6748ff,#b43ce7,#ef3c9e);box-shadow:0 9px 24px rgba(156,58,226,.2)}.pc242-section-title{display:flex;justify-content:space-between;align-items:center;margin-bottom:11px}.pc242-section-title strong{font-size:16px}.pc242-section-title button{border:0;background:transparent;color:#a778ff;font-size:11px;font-weight:800}.pc242-videos{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.pc242-video{position:relative;aspect-ratio:4/5;border:1px solid #1e2534;border-radius:12px;overflow:hidden;background:#111625;padding:0}.pc242-video img,.pc242-video video{width:100%;height:100%;object-fit:cover}.pc242-view{position:absolute;left:8px;bottom:7px;color:#fff;font-size:9px;font-weight:900;z-index:2}.pc242-empty{padding:44px 12px;text-align:center;color:#7e8799;border:1px dashed #20283a;border-radius:14px;grid-column:1/-1}.pc242-other{display:grid;grid-template-columns:1.5fr 1fr 46px;gap:8px;margin-top:18px}.pc242-btn:disabled{opacity:.6}.pc242 nav{margin:0}`; document.head.appendChild(s); }
async function loadProfile(userId) { const clean=String(userId||'').replace(/^@/,'').trim(); if(!clean) return null; const d=await api(`/api/account/profile/${encodeURIComponent(clean)}`); return d?.profile ? {...d.profile,stats:d.stats||{}} : null; }
async function videos(uid) { if(!uid) return []; try { const d=await api('/api/media/videos?limit=50',{auth:false}); return Array.isArray(d.videos) ? d.videos.filter(v=>String(v.ownerUid||'')===String(uid)) : []; } catch { return []; } }
function videoGrid(items){ if(!items.length) return '<div class="pc242-empty">No videos uploaded yet.</div>'; return items.slice(0,9).map(v=>`<button class="pc242-video" type="button" data-video="${esc(v.secureUrl||v.videoUrl||v.url||'')}" data-title="${esc(v.title||'')}">${v.thumbnailUrl?`<img src="${esc(v.thumbnailUrl)}" alt="" loading="lazy">`:`<video src="${esc(v.secureUrl||v.videoUrl||v.url||'')}" muted playsinline preload="metadata"></video>`}<span class="pc242-view">${esc(count(v.views))} views</span></button>`).join(''); }
function openVideo(url,title){ if(!url)return; const o=document.createElement('div'); o.style.cssText='position:fixed;inset:0;z-index:30000;background:#000;display:grid;place-items:center'; o.innerHTML=`<div style="position:relative;width:min(100%,520px);height:100vh"><button style="position:absolute;z-index:2;top:14px;left:14px;width:38px;height:38px;border:0;border-radius:50%;background:rgba(0,0,0,.7);color:#fff;font-size:24px">×</button><div style="position:absolute;z-index:2;top:20px;left:64px;color:#fff;font-size:13px;font-weight:800">${esc(title||'Indo video')}</div><video src="${esc(url)}" controls autoplay playsinline style="width:100%;height:100%;object-fit:contain"></video></div>`; document.body.appendChild(o); o.querySelector('button').onclick=()=>o.remove(); }
async function follow(uid,b){ try { const st=await api(`/api/social/follow-status/${encodeURIComponent(uid)}`); const next=!Boolean(st.following); b.disabled=true; const d=await api('/api/social/follow',{method:'POST',body:JSON.stringify({targetUid:uid,follow:next})}); b.textContent=d.pending?'Requested':d.following?'Following':'Follow'; } catch(e){ b.textContent=e.message||'Follow'; } finally { b.disabled=false; } }

export async function renderProfile(app, profileArg=null) {
  styles(); const currentUid=String(auth.currentUser?.uid||''); const requestedUid=String(profileArg?.uid||profileArg?.ownerUid||'').trim(); const requestedId=String(profileArg?.userId||profileArg?.username||'').replace(/^@/,'').trim(); let profile=null; let own=false;
  try { if(requestedUid&&requestedUid===currentUid) own=true; else if(requestedId){ profile=await loadProfile(requestedId); own=Boolean(currentUid&&profile?.uid&&String(profile.uid)===currentUid); } else own=true; if(own){ const me=auth.currentUser; const fallback=String(me?.displayName||me?.email?.split('@')[0]||'user').replace(/^@/,''); profile=await loadProfile(profile?.userId||fallback).catch(()=>null)||profile||{uid:currentUid,userId:fallback,name:me?.displayName||fallback}; } if(!profile) throw new Error('Profile could not be loaded.'); } catch(e) { app.innerHTML=`<div class="pc242"><main class="pc242-main"><h2>Profile unavailable</h2><p>${esc(e.message)}</p></main>${nav('profile')}</div>`; return; }
  const uid=String(profile.uid||profile.ownerUid||currentUid); const id=String(profile.userId||profile.username||'').replace(/^@/,''); const name=String(profile.name||'Indo User'); const bio=String(profile.bio||profile.about||'').trim(); const location=String(profile.location||'').trim(); const vcount=Number(profile.videosCount??profile.stats?.videosCount??profile.postsCount??0); const followers=Number(profile.followersCount??profile.stats?.followersCount??0); const following=Number(profile.followingCount??profile.stats?.followingCount??0); const likes=Number(profile.likesCount??profile.stats?.likesCount??0); const items=await videos(uid);
  app.innerHTML=`<div class="pc242">${renderIndoBrandTopbar({rightLabel:'Profile'})}<main class="pc242-main"><section class="pc242-hero"><div class="pc242-pic"><div class="pc242-ring"></div>${avatar(profile)}<span class="pc242-online"></span></div><div class="pc242-name">${esc(name)}${profile.verified?'<span class="pc242-verified">✓</span>':''}</div><div class="pc242-id">@${esc(id)}</div>${bio?`<div class="pc242-bio">${esc(bio)}</div>`:''}${location?`<div class="pc242-location">⌖ ${esc(location)}</div>`:''}</section><div class="pc242-line"></div><section class="pc242-stats"><button class="pc242-stat"><b>${count(vcount)}</b><span>Videos</span></button><button class="pc242-stat"><b>${count(followers)}</b><span>Followers</span></button><button class="pc242-stat"><b>${count(following)}</b><span>Following</span></button><button class="pc242-stat"><b>${count(likes)}</b><span>Likes</span></button></section>${own?`<div class="pc242-actions"><button class="pc242-btn primary" type="button" data-screen="edit-profile">✎ Edit Profile</button><button class="pc242-btn" type="button" data-profile-share>Share</button><button class="pc242-btn" type="button" data-screen="settings">⚙</button></div>`:`<div class="pc242-other"><button class="pc242-btn primary" type="button" data-follow="${esc(uid)}">Follow</button><button class="pc242-btn" type="button" data-screen="messages">Message</button><button class="pc242-btn" type="button" data-profile-share>↗</button></div>`}<section><div class="pc242-section-title"><strong>${own?'Recent Videos':'Videos'}</strong><button type="button" data-screen="video">View all</button></div><div class="pc242-videos">${videoGrid(items)}</div></section></main>${nav('profile')}</div>`;
  app.querySelectorAll('[data-video]').forEach(b=>b.addEventListener('click',()=>openVideo(b.dataset.video,b.dataset.title))); app.querySelectorAll('[data-profile-share]').forEach(b=>b.addEventListener('click',async()=>{const url=`${location.origin}${location.pathname}?profile=${encodeURIComponent(id)}`;try{if(navigator.share)await navigator.share({title:`${name} on Indo`,url});else if(navigator.clipboard)await navigator.clipboard.writeText(url);}catch{}})); app.querySelectorAll('[data-follow]').forEach(b=>b.addEventListener('click',()=>follow(b.dataset.follow,b)));
}
