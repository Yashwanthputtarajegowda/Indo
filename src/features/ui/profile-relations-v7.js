import { state } from '../../state.js';
import { auth } from '../auth/firebase-client.js';

const KEY = Symbol.for('indo.profileRelationsV7');
function esc(value = '') { return String(value).replace(/[&<>\"']/g, (c) => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '\"':'&quot;', "'":'&#039;' }[c])); }
function styleOnce() { if (document.getElementById('indo-profile-relations-v7-style')) return; const style=document.createElement('style'); style.id='indo-profile-relations-v7-style'; style.textContent=`.indo-rel-v7{position:fixed;inset:0;z-index:35000;background:rgba(0,0,0,.78);display:grid;place-items:center;padding:14px}.indo-rel-v7-card{width:min(100%,520px);height:min(80vh,640px);background:#101016;border:1px solid #282830;border-radius:16px;overflow:hidden;display:flex;flex-direction:column;box-shadow:0 18px 60px rgba(0,0,0,.65)}.indo-rel-v7-head{height:56px;flex:0 0 56px;display:flex;align-items:center;justify-content:space-between;padding:0 16px;border-bottom:1px solid #24242b;color:#fff}.indo-rel-v7-head strong{font-size:15px}.indo-rel-v7-close{width:34px;height:34px;border:0;border-radius:50%;background:#1b1b22;color:#fff;font-size:22px;cursor:pointer}.indo-rel-v7-list{flex:1;overflow:auto;padding:8px}.indo-rel-v7-row{width:100%;display:flex;align-items:center;gap:12px;padding:11px 10px;min-height:60px;border:0;border-radius:10px;background:transparent;color:#fff;text-align:left;cursor:pointer}.indo-rel-v7-row:hover{background:#1a1a21}.indo-rel-v7-avatar{width:40px;height:40px;min-width:40px;border-radius:50%;display:grid;place-items:center;background:#2a2a31;font-weight:800}.indo-rel-v7-name{font-size:13px;font-weight:700;line-height:1.2}.indo-rel-v7-id{font-size:11px;color:#92929d;margin-top:2px}.indo-rel-v7-empty{padding:36px 16px;text-align:center;color:#8d8d98;font-size:13px}`; document.head.appendChild(style); }
function closeList(){ document.querySelector('.indo-rel-v7')?.remove(); }
async function authRequest(path){ const user=auth.currentUser; if(!user) throw new Error('Please login first.'); const token=await user.getIdToken(); const apiBase=window.INDO_API_BASE||''; return fetch(`${apiBase}${path}`,{headers:{Authorization:`Bearer ${token}`}}); }
function getTargetUid(){ const profile=state.profile||{}; return String(profile.uid||profile.ownerUid||profile.userId||auth.currentUser?.uid||'').trim(); }
async function fetchRelation(targetUid,relation){ const response=await authRequest(`/api/social/${encodeURIComponent(relation)}/${encodeURIComponent(targetUid)}`); const data=await response.json().catch(()=>({})); if(!response.ok) throw new Error(data.error||`Could not load ${relation}.`); return {count:Number(data.count||(Array.isArray(data.items)?data.items.length:0)),items:Array.isArray(data.items)?data.items:[]}; }
async function refreshStats(root=document.getElementById('root')){ if(!root)return; const fb=root.querySelector('.profile-direct-stat[data-relation="followers"]'), fw=root.querySelector('.profile-direct-stat[data-relation="following"]'); if(!fb&&!fw)return; const targetUid=getTargetUid(); if(!targetUid)return; const seen=fb?.dataset.relationCountUid===targetUid&&fw?.dataset.relationCountUid===targetUid; if(seen)return; try{ const [followers,following]=await Promise.all([fetchRelation(targetUid,'followers'),fetchRelation(targetUid,'following')]); const fbc=fb?.querySelector('[data-followers-count]'),fwc=fw?.querySelector('[data-following-count]'); if(fbc)fbc.textContent=String(followers.count); if(fwc)fwc.textContent=String(following.count); if(fb)fb.dataset.relationCountUid=targetUid; if(fw)fw.dataset.relationCountUid=targetUid; }catch(error){ console.warn('Profile relation count refresh failed:',error); } }
async function openRelation(targetUid,relation){ closeList(); styleOnce(); const modal=document.createElement('div'); modal.className='indo-rel-v7'; modal.innerHTML=`<section class="indo-rel-v7-card"><header class="indo-rel-v7-head"><strong>${relation==='followers'?'Followers':'Following'}</strong><button class="indo-rel-v7-close" type="button" aria-label="Close">×</button></header><div class="indo-rel-v7-list"><div class="indo-rel-v7-empty">Loading...</div></div></section>`; document.body.appendChild(modal); modal.querySelector('.indo-rel-v7-close')?.addEventListener('click',closeList); modal.addEventListener('click',e=>{if(e.target===modal)closeList();}); const list=modal.querySelector('.indo-rel-v7-list'); try{const result=await fetchRelation(targetUid,relation); if(!result.items.length){list.innerHTML='<div class="indo-rel-v7-empty">No users yet.</div>';return;} list.innerHTML=result.items.map(item=>{const uid=String(item.uid||'').trim(),userId=String(item.userId||item.username||'').replace(/^@/,''),name=String(item.name||'Indo User'),initial=(name.trim().charAt(0)||userId.charAt(0)||'U').toUpperCase(); return `<button class="indo-rel-v7-row" type="button" data-rel-uid="${esc(uid)}" data-rel-user="${esc(userId)}"><div class="indo-rel-v7-avatar">${esc(initial)}</div><div><div class="indo-rel-v7-name">${esc(name)}</div><div class="indo-rel-v7-id">@${esc(userId||'user')}</div></div></button>`;}).join(''); list.querySelectorAll('[data-rel-uid]').forEach(button=>button.addEventListener('click',async()=>{const uid=button.dataset.relUid||'',username=button.dataset.relUser||'';closeList();state.profile={uid,ownerUid:uid,username};state.screen='profile';if(window.__indoNavigate)await window.__indoNavigate('profile');})); }catch(error){list.innerHTML=`<div class="indo-rel-v7-empty">${esc(error?.message||'Could not load list.')}</div>`;} }
function install(){ if(globalThis[KEY])return; globalThis[KEY]=true; styleOnce(); document.addEventListener('click',event=>{const target=event.target instanceof Element?event.target.closest('.profile-direct-stat[data-relation]'):null; if(!target)return; const relation=target.dataset.relation; if(relation!=='followers'&&relation!=='following')return; const root=document.getElementById('root'); if(!root?.contains(target))return; const targetUid=getTargetUid(); if(!targetUid)return; event.preventDefault(); event.stopImmediatePropagation(); openRelation(targetUid,relation); },true); const root=document.getElementById('root')||document.body; new MutationObserver(()=>refreshStats()).observe(root,{childList:true,subtree:true}); setTimeout(refreshStats,0); setTimeout(refreshStats,300); setTimeout(refreshStats,1000); }
install(); export { install };
