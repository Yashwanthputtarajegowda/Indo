// Production runtime configuration.
// Set window.INDO_API_BASE_URL before app.js loads when frontend/backend
// are deployed on different origins. Keep /api for same-origin proxy setups.
window.INDO_API_BASE_URL = window.INDO_API_BASE_URL || "/api";
